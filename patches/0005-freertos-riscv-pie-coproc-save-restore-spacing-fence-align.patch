diff --git a/components/freertos/FreeRTOS-Kernel/portable/riscv/portasm.S b/components/freertos/FreeRTOS-Kernel/portable/riscv/portasm.S
--- a/components/freertos/FreeRTOS-Kernel/portable/riscv/portasm.S
+++ b/components/freertos/FreeRTOS-Kernel/portable/riscv/portasm.S
@@ -270,20 +270,63 @@
  * @brief Macros to save and restore the PIE coprocessor registers to and from the given frame
  */
 .macro pie_save_regs frame=a0
+    /* ==== iridium_acars patch 0005 — PIE-wedge spacing/alignment/fence screen ====
+     * Combined, falsifiable experiment: does ANY instruction spacing, memory
+     * fence, or 4-byte alignment stop the deterministic HP-WDT wedge that parks
+     * Core 1 inside rtos_save_pie_coproc on the back-to-back
+     * esp.vst.128.ip / esp.vld.128.ip Q-register run?
+     * Precedent: esp-dsp issue #102 (DSP-158) — on ESP32-P4 rev-1.0 silicon a
+     * custom-coproc op (esp.lp.setup) that is not 4-byte aligned makes the core
+     * "go haywire"; the fix is `.balignw 4,0x0001` (pad with a c.nop). We test
+     * three hypotheses at once here and in pie_restore_regs:
+     *   H1 alignment  : `.balignw 4,0x0001` before each coproc-op run.
+     *   H2 ordering   : `fence` before the first vector access and between the
+     *                   Q-register block and the exotic-register block.
+     *   H3 pipeline   : `nop`s separating consecutive esp.v*.128.ip / QACC ops.
+     * ONLY fences/alignment/nops are added — every functional PIE op is kept
+     * intact and in its original order. If the screen passes, a follow-up
+     * bisects which element mattered. fence/nop/.balignw are all register-safe
+     * (no clobber of a1/a2 used below). See patches/README.md 0005. */
+    fence                        /* H2: order vs the freshly-written save area */
+    .balignw 4,0x0001            /* H1: 4-byte-align the vst run (esp-dsp #102) */
     /* Save the 128-bit Q registers from the frame memory and then frame += 16 */
     esp.vst.128.ip  q0, \frame, 16
+    nop                          /* H3: separate back-to-back esp.vst.128.ip */
+    nop
     esp.vst.128.ip  q1, \frame, 16
+    nop
+    nop
     esp.vst.128.ip  q2, \frame, 16
+    nop
+    nop
     esp.vst.128.ip  q3, \frame, 16
+    nop
+    nop
     esp.vst.128.ip  q4, \frame, 16
+    nop
+    nop
     esp.vst.128.ip  q5, \frame, 16
+    nop
+    nop
     esp.vst.128.ip  q6, \frame, 16
+    nop
+    nop
     esp.vst.128.ip  q7, \frame, 16
+    fence                        /* H2: order between Q-reg block and QACC block */
+    .balignw 4,0x0001            /* H1: 4-byte-align the QACC/UA run */
     /* Save the QACC_H and QACC_L registers, each being 256 bits big */
     esp.st.qacc.l.l.128.ip \frame, 16
+    nop                          /* H3: separate back-to-back QACC/UA ops */
+    nop
     esp.st.qacc.l.h.128.ip \frame, 16
+    nop
+    nop
     esp.st.qacc.h.l.128.ip \frame, 16
+    nop
+    nop
     esp.st.qacc.h.h.128.ip \frame, 16
+    nop
+    nop
     /* UA_STATE register (128 bits) */
     esp.st.ua.state.ip \frame, 16
     /* XACC register (40 bits)
@@ -322,6 +365,8 @@
      */
 
     /* Pointer not increased to write data as tightly as possible (minimum increment is 8). */
+    fence                        /* iridium_acars 0005 H2: order before the XACC op */
+    .balignw 4,0x0001            /* iridium_acars 0005 H1: align the exotic XACC op */
     esp.st.u.xacc.ip \frame, 0
 
     /* SAR_BYTES and FFT_BIT_WIDTH registers
@@ -341,24 +386,57 @@
 
 
 .macro pie_restore_regs frame=a0
+    /* iridium_acars patch 0005 — mirror of the pie_save_regs spacing/alignment/
+     * fence screen (H1 .balignw / H2 fence / H3 nop). The confirmed wedge PC is
+     * the 4th vld here (esp.vld.128.ip q3); see the header comment on
+     * pie_save_regs and patches/README.md 0005. Only fences/alignment/nops are
+     * added — the functional PIE ops are unchanged and in original order. */
+    fence                        /* H2: order vs the freshly-written save area */
+    .balignw 4,0x0001            /* H1: 4-byte-align the vld run (esp-dsp #102) */
     /* Restore the 128-bit Q registers from the frame memory and then frame += 16 */
     esp.vld.128.ip  q0, \frame, 16
+    nop                          /* H3: separate back-to-back esp.vld.128.ip */
+    nop
     esp.vld.128.ip  q1, \frame, 16
+    nop
+    nop
     esp.vld.128.ip  q2, \frame, 16
+    nop
+    nop
     esp.vld.128.ip  q3, \frame, 16
+    nop
+    nop
     esp.vld.128.ip  q4, \frame, 16
+    nop
+    nop
     esp.vld.128.ip  q5, \frame, 16
+    nop
+    nop
     esp.vld.128.ip  q6, \frame, 16
+    nop
+    nop
     esp.vld.128.ip  q7, \frame, 16
+    fence                        /* H2: order between Q-reg block and QACC block */
+    .balignw 4,0x0001            /* H1: 4-byte-align the QACC/UA run */
     /* Save the QACC_H and QACC_L registers, each being 256 bits big */
     esp.ld.qacc.l.l.128.ip \frame, 16
+    nop                          /* H3: separate back-to-back QACC/UA ops */
+    nop
     esp.ld.qacc.l.h.128.ip \frame, 16
+    nop
+    nop
     esp.ld.qacc.h.l.128.ip \frame, 16
+    nop
+    nop
     esp.ld.qacc.h.h.128.ip \frame, 16
+    nop
+    nop
     /* UA_STATE register (128 bits) */
     esp.ld.ua.state.ip \frame, 16
     /* XACC register (40 bits)
      * Pointer not increased because the minimum step is 8, preventing data loss */
+    fence                        /* iridium_acars 0005 H2: order before the XACC op */
+    .balignw 4,0x0001            /* iridium_acars 0005 H1: align the exotic XACC op */
     esp.ld.xacc.ip \frame, 0
 
     /* The following registers are packed in the same word (addr: frame + 4):
