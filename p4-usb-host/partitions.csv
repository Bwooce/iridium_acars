# ESP32-P4 partition table for the iridium ACARS firmware.
#
# Board: Waveshare ESP32-P4-Nano with GigaDevice GD25Q128 16 MB SPI flash.
# OTA-capable layout (D19): two 6 MB app slots + otadata, leaves ~2.5 MB
# for the SPIFFS storage partition.
#
# 6 MB per slot is sized to accommodate the dev build with the
# SMOKE_TEST_RAW_IRIDIUM fixture embedded (~5.9 MB). The shipping
# (no-smoke) build is ~1 MB, so there's plenty of headroom for the
# Wi-Fi/HTTP/OTA layer to grow.
#
# OTA flow: bootloader picks ota_0 or ota_1 based on otadata; on a
# successful POST /ota the new image is staged into the inactive
# slot, otadata is flipped, and the device reboots. If the new
# firmware doesn't call esp_ota_mark_app_valid_cancel_rollback()
# within the rollback window, the bootloader reverts to the
# previous slot on next boot. Brick-recovery is the USB cable.
#
# Name,    Type, SubType, Offset,   Size,     Flags
nvs,       data, nvs,     0x9000,   0x6000,
otadata,   data, ota,     0xf000,   0x2000,
phy_init,  data, phy,     0x11000,  0x1000,
ota_0,     app,  ota_0,   0x20000,  6M,
ota_1,     app,  ota_1,   ,         6M,
storage,   data, spiffs,  ,         2500K,
