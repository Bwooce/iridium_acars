/*
 * libacars_idf — static version.c replacement.
 *
 * Upstream libacars regenerates this file via version.cmake at every
 * CMake configure step using `git describe`. We vendor libacars as a
 * frozen tree, so a static literal is sufficient. Update LA_VERSION
 * if/when you bump the vendored libacars to a new release.
 */
char const * const LA_VERSION = "2.2.1-idf";
